import AIMatchingDashboard from "@/components/matching/AIMatchingDashboard";
import { DashboardLayout } from "@/components/DashboardLayout";

const AIMatching = () => {
  return (
    <DashboardLayout>
      <AIMatchingDashboard />
    </DashboardLayout>
  );
};

export default AIMatching;